import { createMuiTheme } from '@material-ui/core';

export default createMuiTheme({
  palette: {
    primary: {
      main: '#48528C',
    },
    secondary: {
      main: '#F3F6FD',
    },
    accent: {
      backgroundColor: '#000000',
    }
  },
});
